/**
 * Format options.
 */
export interface FormatDsvOptions {
  /**
   * The delimiter between values, default `','`.
   */
  delimiter: string

  /**
   * The enclosing string, default `'"'`.
   */
  enclosing: string
}
